import 'package:flutter/material.dart';

class DashboardCurrentFocus extends StatelessWidget {
  const DashboardCurrentFocus({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 160,
    );
  }
}
